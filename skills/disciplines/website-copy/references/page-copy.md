# Page-copy working format

For each section capture:

- section job;
- primary idea;
- final copy;
- supporting proof IDs;
- primary action;
- implementation notes only when content behavior depends on them;
- unresolved content request.

Keep alternative headlines in a clearly separate appendix so implementation does not accidentally mix variants.
